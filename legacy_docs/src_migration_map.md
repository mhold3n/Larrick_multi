# Src Migration Map (Legacy Copy)

The following legacy top-level source directories were moved under `src/`:

- `campro` -> `src/campro`
- `Simulations` -> `src/Simulations`
- `truthmaker` -> `src/truthmaker`
- `larrak` -> `src/larrak`
- `webapp` -> `src/webapp`
- `dashboard` -> `src/dashboard`
- `worker` -> `src/worker`
- `interpreter` -> `src/interpreter`
- `provenance` -> `src/provenance`

Non-source assets remain at root (`scripts/`, `tests/`, `models/`, `output/`, `data/`, etc.) to keep workflow scripts and artifacts visible.
