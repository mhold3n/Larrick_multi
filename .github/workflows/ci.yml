name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fast-checks:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          lfs: true
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('pyproject.toml', 'requirements-integration.txt', 'setup.cfg') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-integration.txt
          pip install -e ".[dev]" --no-deps
      - name: Ruff format check
        run: ruff format --check src tests scripts
      - name: Ruff lint
        run: ruff check src tests scripts
      - name: Mypy
        env:
          PYTHONPATH: src
        run: >
          mypy
          src/larrak2/architecture/workflow_contracts.py
          src/larrak2/cli/run.py
          src/larrak2/cli/train.py
      - name: Pytest
        env:
          PYTHONPATH: src
          LARRICK_MULTI_ROOT: ${{ github.workspace }}
        run: pytest -q
